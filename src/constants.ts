export const ADDON_ID = 'storybook-addon-determinism';
export const PARAM_KEY = 'determinism';
export const GLOBAL_KEY = 'determinism';

/** Default PRNG seed when the parameter enables determinism without one. */
export const DEFAULT_SEED = 1;
